import Link from 'next/link'
import Navbar from '../../components/Navbar'

export default function PaymentCancel() {
  return (
    <div className="min-h-screen bg-gray-50">
      <Navbar />
      <div className="max-w-md mx-auto px-4 py-24 text-center">
        <div className="text-5xl mb-5">↩️</div>
        <h1 className="font-condensed font-black text-3xl text-gray-900 mb-3">Payment cancelled</h1>
        <p className="text-gray-500 text-sm leading-relaxed mb-8">
          No charge was made. You can upgrade any time from your dashboard.
        </p>
        <Link href="/" className="btn-secondary inline-block">Back to home</Link>
      </div>
    </div>
  )
}
